    <% if current_user %>
      Welcome Back <%= current_user.first_name %> ! | <%= link_to "Logout", logout_path %>
    <% else %>

    <!-- Button trigger modal -->
      <%= render partial: "sessions/login"%> | <%= render partial: "users/new"%>
    <% end %>